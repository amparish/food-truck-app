	  <div class="navbar-fixed header">
	    <nav>
	      <div class="nav-wrapper">
		    <ul id="slide-out" class="side-nav">
		      <li><a href="#!">First Sidebar Link</a></li>
		      <li><a href="#!">Second Sidebar Link</a></li>
		    </ul>
	    	<a href="#" data-activates="slide-out" class="button-collapse show-on-large left"><i class="material-icons menu ">menu</i>
	    	</a>
	      </div>
	    </nav>
	  </div>
	<div class="main-content">
		<h1>TRUCKS</h1>
		<hr>
		{{#each foodTruck}}
			<div class="row">
				<form action="/browse/truck/{{this.id}}" method="GET">
					<button type="submit" class="truck">
						<div class="row">
							<div class="col s3">
							<img src={{this.photo}} class="responsive-img">								
							</div>
							<div class="col s9">
								<div class="row font-white">
									<h1 class="companyTitle">{{this.companyName}}</h1>
								</div>
								<div class="row font-white">
									<P>{{this.description}}</P>	
								</div>						
							</div>
						</div>
					</button>
				</form>
			</div>
		{{/each}}					
	</div>

